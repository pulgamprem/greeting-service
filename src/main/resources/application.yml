management:
  endpoints:
    enabled-by-default: true
  info:
    enabled: true
server:
  port: 5000

feign:
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 5000
        loggerLevel: basic
integration:
  concat:
    url: http://concatservice-env.eba-3b3prm7q.us-east-2.elasticbeanstalk.com/concat
  hello:
    url: http://helloService-env.eba-fxvkijqe.us-east-2.elasticbeanstalk.com/hello